apiVersion: v1
kind: Secret
metadata:
  name: secrets-do
type: Opaque
stringData:
  config.yaml: |-
    DIGITAL_OCEAN_TOKEN: {{ .Values.secrets.digitalOceanToken }}
    EXTERNAL_DNS_DOMAIN: {{ .Values.secrets.externalDnsDomain }}
